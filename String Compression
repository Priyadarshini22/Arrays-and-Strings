string=input()  
out=[] 
i=0 
while i<len(string):
     temp=string[i]  
     count=0
     while i<len(string): 
         if temp in string[i]: 
             count+=1  
             i+=1
         else: 
             break 
     out.append(temp) 
     out.append(count) 
print(*out,sep="")
